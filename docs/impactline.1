.nh
.TH "IMPACTLINE" "1" "Aug 2026" "Impactline" "Impactline Manual"

.SH NAME
impactline - Estimate the blast radius of a code change


.SH SYNOPSIS
\fBimpactline [flags]\fP


.SH DESCRIPTION
Impactline analyzes a Git repository and estimates the impact of changing
a given file or directory, using deterministic evidence such as
dependency graphs, Git history, and CI configuration.

.PP
Canonical usage:
  impactline inspect 

.PP
Convenience alias (root command):
  impactline 

.PP
Resolution order for the alias:
  1. If the first argument matches a registered subcommand name exactly
     (diff, graph, doctor, history, version, completion, inspect), it is
     dispatched to that subcommand.
  2. Otherwise, if the first argument resolves to an existing path in the
     working tree, it is treated as "impactline inspect ".
  3. Otherwise, impactline errors with "unknown command or path" rather
     than silently guessing.

.PP
CI pipelines and scripts should prefer the canonical "impactline
inspect" form to avoid any ambiguity.


.SH OPTIONS
\fB--fail-on\fP=""
	exit with code 2 if risk is at or above this level (low, medium, high)

.PP
\fB-h\fP, \fB--help\fP[=false]
	help for impactline

.PP
\fB--json\fP[=false]
	output machine-readable JSON

.PP
\fB-o\fP, \fB--output\fP=""
	write the report to this file instead of stdout


.SH SEE ALSO
\fBimpactline-diff(1)\fP, \fBimpactline-doctor(1)\fP, \fBimpactline-graph(1)\fP, \fBimpactline-history(1)\fP, \fBimpactline-inspect(1)\fP, \fBimpactline-version(1)\fP


.SH HISTORY
27-Aug-2026 Auto generated by spf13/cobra
