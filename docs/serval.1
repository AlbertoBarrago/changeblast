.nh
.TH "SERVAL" "1" "Aug 2026" "Serval" "Serval Manual"

.SH NAME
serval - Estimate the blast radius of a code change


.SH SYNOPSIS
\fBserval [flags]\fP


.SH DESCRIPTION
Serval analyzes a Git repository and estimates the impact of changing
a given file or directory, using deterministic evidence such as
dependency graphs, Git history, and CI configuration.

.PP
Canonical usage:
  serval inspect 

.PP
Convenience alias (root command):
  serval 

.PP
Resolution order for the alias:
  1. If the first argument matches a registered subcommand name exactly
     (diff, graph, doctor, history, version, completion, inspect), it is
     dispatched to that subcommand.
  2. Otherwise, if the first argument resolves to an existing path in the
     working tree, it is treated as "serval inspect ".
  3. Otherwise, serval errors with "unknown command or path" rather
     than silently guessing.

.PP
CI pipelines and scripts should prefer the canonical "serval
inspect" form to avoid any ambiguity.


.SH OPTIONS
\fB--fail-on\fP=""
	exit with code 2 if risk is at or above this level (low, medium, high)

.PP
\fB-h\fP, \fB--help\fP[=false]
	help for serval

.PP
\fB--json\fP[=false]
	output machine-readable JSON

.PP
\fB-o\fP, \fB--output\fP=""
	write the report to this file instead of stdout


.SH SEE ALSO
\fBserval-diff(1)\fP, \fBserval-doctor(1)\fP, \fBserval-graph(1)\fP, \fBserval-history(1)\fP, \fBserval-inspect(1)\fP, \fBserval-version(1)\fP


.SH HISTORY
27-Aug-2026 Auto generated by spf13/cobra
