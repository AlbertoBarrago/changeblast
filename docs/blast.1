.nh
.TH "BLAST" "1" "Aug 2026" "ChangeBlast" "ChangeBlast Manual"

.SH NAME
blast - Estimate the blast radius of a code change


.SH SYNOPSIS
\fBblast [flags]\fP


.SH DESCRIPTION
ChangeBlast (blast) analyzes a Git repository and estimates the impact of
changing a given file or directory, using deterministic evidence such as
dependency graphs, Git history, and CI configuration.

.PP
Canonical usage:
  blast inspect 

.PP
Convenience alias (root command):
  blast 

.PP
Resolution order for the alias:
  1. If the first argument matches a registered subcommand name exactly
     (diff, graph, doctor, history, version, completion, inspect), it is
     dispatched to that subcommand.
  2. Otherwise, if the first argument resolves to an existing path in the
     working tree, it is treated as "blast inspect ".
  3. Otherwise, blast errors with "unknown command or path" rather than
     silently guessing.

.PP
CI pipelines and scripts should prefer the canonical "blast inspect" form
to avoid any ambiguity.


.SH OPTIONS
\fB--fail-on\fP=""
	exit with code 2 if risk is at or above this level (low, medium, high)

.PP
\fB-h\fP, \fB--help\fP[=false]
	help for blast

.PP
\fB--json\fP[=false]
	output machine-readable JSON


.SH SEE ALSO
\fBblast-diff(1)\fP, \fBblast-doctor(1)\fP, \fBblast-graph(1)\fP, \fBblast-history(1)\fP, \fBblast-inspect(1)\fP, \fBblast-version(1)\fP


.SH HISTORY
26-Aug-2026 Auto generated by spf13/cobra
